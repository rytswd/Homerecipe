#!/usr/bin/env bash

# Managing nvm via Homebrew is unsupported
#   `brew info nvm` for more information

# Recommended installation:
#curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.1/install.sh | bash

# WARNING: Default recipe does use Homebrew for simplicity sake

[[ -f ~/.nvm ]] || mkdir ~/.nvm
